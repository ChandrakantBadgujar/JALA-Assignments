package _12_Constructor;

public class _05_Multiple_type_Constructor_with_sameObject {
	
	/*In Java, it is not possible to call a constructor multiple times on the same object. 
	 * Once an object is created, its constructor is called only once, during the creation of the object.
	 * If you try to call the constructor again on the same object, you will get a compilation error, 
	 * because the constructor can only be called during the creation of the object.
	 * 
	 */

}
